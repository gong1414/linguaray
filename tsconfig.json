{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "jsxImportSource": "solid-js",

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    /* Path alias for the workspace @linguaray/ui package */
    "baseUrl": ".",
    "paths": {
      "@linguaray/ui": ["./packages/ui/src/index.ts"]
    }
  },
  "include": ["src", "test"],
  // Migration (2026-08-16): the React 19 tree (jsx: react-jsx) has its own
  // project file — this config stays jsx: preserve + solid-js until Phase 5.
  // Not a project reference: noEmit projects cannot be referenced. The
  // typecheck script typechecks both configs explicitly.
  "exclude": ["src/app"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
