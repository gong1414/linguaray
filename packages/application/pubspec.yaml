name: linguaray_application
description: Pure Dart application contracts and use cases for LinguaRay.
version: 0.1.0
publish_to: "none"
resolution: workspace

environment:
  sdk: ">=3.13.1 <4.0.0"

dependencies:
  pub_semver: ^2.2.0

dev_dependencies:
  test: ^1.25.0
