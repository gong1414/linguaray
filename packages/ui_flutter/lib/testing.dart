/// Golden-test support shared by the kit's and the app's golden suites.
///
/// Test-only helpers; nothing under `lib/` outside `src/testing/` should
/// import the implementation directly.
library;

export 'src/testing/golden_fonts.dart';
