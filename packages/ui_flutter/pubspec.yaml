name: linguaray_ui
description: The reusable Flutter design system used by LinguaRay.
version: 0.1.0
publish_to: "none"
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  fluentui_system_icons: ^1.1.255

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
