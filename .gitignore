node_modules
dist

# UI Lab review screenshots (local verification evidence, not source)
s1b-shots/

# ZCode agent runtime artifacts
.zcode/

# Mimosa security-scan artifacts
.mimosa/
src-tauri/.mimosa/

# Local git worktrees (isolated dev workspaces)
.worktrees/

# Local pnpm content-addressable store (never commit)
.pnpm-store/

# Internal design / implementation plans (not part of the product tree)
docs/superpowers/
design-system/
storybook-static/
