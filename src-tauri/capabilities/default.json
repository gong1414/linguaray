{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "popup", "input"],
  "permissions": [
    "core:default",
    "opener:default",
    "global-shortcut:default",
    "store:default"
  ]
}
