{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "popup",
  "description": "Capability for the popup window — hides via window API (core:default does NOT include allow-hide)",
  "windows": ["popup"],
  "permissions": [
    "core:default",
    "core:window:allow-hide"
  ]
}
